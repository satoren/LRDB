// TODO.
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lua",
            "request": "launch",
            "name": "Lua-Debug",
            "host": "localhost",
            "port": 21110,
            "sourceRoot": "${workspaceRoot}",
            "startupCommand": {
                "program": "${command.DefaultLuaInterpreter}",
                "args": [
                    "--port",
                    "21110",
                    "${command.CurrentSource}"
                ],
                "cwd": "${workspaceRoot}"
            },
            "stopOnEntry": true
        }
    ]
}